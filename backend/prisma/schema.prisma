datasource db {
    provider = "sqlite"
    url      = env("DATABASE_URL")
}

generator client {
    provider = "prisma-client-js"
}
model User {
    id       Int    @id
    email    String @unique
    username String @unique
    password String
}
