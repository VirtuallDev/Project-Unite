model User {
    id           String    @id @unique
    email        String    @unique
    username     String    @unique
    createdAt    DateTime?
    hashSalt     String?
    password     String
    refreshToken String?
    issuedAt     BigInt?
}
